check hdmi audio output flow. it seems some frequencies like bass sound like they are filtered out - try find reasons and fix. replace of AY core to
  something more naturally sounding is allowed also

****

Found and fixed, and it was not the HDMI path: every AY tone was an octave low.

The cause is one pin. aberrant.v tied the three YM2149 cores' SEL high. In ym2149.sv SEL selects the prescaler: 0 divides the clock enable by 8, which is what a real AY does (tone = fclk / (16 x period)), and 1 divides by 16, meant for a core fed at twice the chip clock - which is how the MiSTer ZX Spectrum drives it, a 3.5 MHz enable for a 1.7734 MHz chip. Here the enable is the chip clock itself, 1.7734 MHz, so every tone counter and the envelope generator ran at half rate.

Measured in a standalone simulation of the core, then in the unit test: with SEL=1, tone period 252 gives 220 Hz and an envelope of period 1000 gives 3.5 Hz; with SEL=0 they give 440 Hz and 6.9 Hz, which is what the chip does. A PT3 bass line at 65-130 Hz an octave down is 33-65 Hz, which a television's speaker cannot reproduce, while the rest of the tune an octave down still sounds like music. That is the "bass filtered out". Nothing in the HDMI path shapes frequency: the encoder takes the mixer word as it is, the resampler is a sample-and-hold, and the DC blocker is still out. The AY core itself is fine and does not need replacing; the flat tables and mixer are the same ones MiSTer plays with.

It also corrects the earlier record. The "1.77 times too fast, nine semitones sharp" figure from when CE was tied high was computed without this pin; through the divide-by-16 it was actually 12% flat. The phase accumulator was still right, it just was not the last step.

Changed: tang/src/aberrant.v - SEL low on all three instances, with the reasoning in a comment and the stale figure corrected. sim/tb/tb_aberrant.v - now counts the output's edges over one simulated second and fails outside 2% of 880, so a wrong prescaler cannot pass make ab-test again; "it oscillates" was not a pitch check. Docs: platform.md, progress.md (defect 12), and a CLAUDE.md trap.

Verified: make ab-test passes at 440 Hz, make lint clean (the one MULTIDRIVEN warning is the pre-existing one on mount_dsk in top.v), make bitstream builds - logic 44%, registers 25%, BSRAM 48%, 0 setup violations, same as before - and bin/tang.fs is the new one. Not heard on a board. Expected on the board: the whole piece up an octave to where it was written, bass audible, envelope effects at double their previous speed.

Next after this is heard: put the DC blocker back (git show f2bb44b^), which is the step progress.md defect 10 is waiting on. Nothing is committed; say the word.

flash it onto flash

****

Written. make flash-fpga-flash (openFPGALoader -f -r) erased and wrote bin/tang.fs to the Tang's SPI flash to 100% and reported Done. Power-cycle the board now - the tool does not reliably reconfigure the chip, so without the power cycle you are still listening to the old bitstream. If the next flash fails with ftdi_usb_reset failed, replug the USB cable first.

what are remaining problems listed?

****

From progress.md, as it stands.

Known defects still open (of twelve, the other eight are marked fixed):
- 4. Clocks the tools cannot reason about. clk_25 and clk_3_12 are counter bits, so timing between them and the PLL is unanalysed; sdram2.v clocks a flop off curs_set and top.v clocks four mount_dsk flops off sd_img_mounted, both data signals; clk27_d is routed on general fabric. Not broken today, but it is why every re-place-and-route is a gamble.
- 10. The DC blocker is out of the signal path. The +3000 to +9000 DC offset under music is real and a sink's AC coupling turns each step into a thump. It is to go back (git show f2bb44b^) once the current build is heard.
- 11. Subpacket layout fix - marked not heard. Stale: you confirmed sound on 1 Sep.
- 12. AY octave (today's) - built and flashed, not yet heard.

Open questions:
- Which bitstream is the shipped one - stale now, bin/tang.fs is built here and is what was just flashed.
- What made tang/rom/uknc_rom.mif (16384 words from a 32256-byte .bin, no converter in the repo).
- What bin2mif's arguments are - binary only, no source.
- Whether the "Disk prot." menu value is an index or a bitmask - never traced through menu.c.
- Why the PPU ends up in a trap loop in simulation - needs an SD card model to go further.
- Will the fixed layout play bipolar - stale, same as defect 11; the answer is now to do defect 10.
- Why the player hangs with "Зависание при приеме а.в.п." - a CPU-PPU channel fault with no probes on the channel; the removed diagnostic monitor would be the tool.

Also outside that list: hold violations (524 this build) are a constraint artefact that nobody has resolved at the source, and S2 does nothing.

Three entries are stale and progress.md should be updated to say sound was heard on 1 Sep; I have not touched it, since this was a question.

what meant by p.4? can you do something so that relayout wont break uknc main screen load from time to time and do not affect floppy load as a result?

****

What defect 4 means. A timing tool checks every path from one clock edge to the next, and it can only do that for nets it knows are clocks. This design had flops clocked by things that are not clocks: a PPU register bit (step), one-cycle pulses out of other flops (clk_dsk, clk_dsk_n, sd_rd, sd_img_mounted), a compare (curs_set). Such a "clock" travels on general routing with whatever delay the placement gives it, the SDC could only call it a made-up 1 us clock, and nothing checked it. The paths were not wrong, they were untested, and each place-and-route re-rolled them. On top of that the MCU's 20 MHz SPI clock on m0s[3] was not declared at all, so the MISO path - the one the power-on handshake and every SD mount goes through - was never analysed either. Those two together are why a re-layout could take the start screen with an SD card in: if the handshake fails the MCU never releases cold reset, and no start screen means no floppy.

Done:
- fdd4.v runs entirely on the 25 MHz clock now. clk_dsk and clk_dsk_n are enables, the track ROM is clocked by clk_25 with the pulse as its chip enable, read_sd's asynchronous clear is synchronous, and step goes through two flops and an edge detector. step was the worst one: it changes on the PPU clock, which is the same counter as clk_25, so it moved at the very edge of the flop it clocked.
- top.v samples sd_img_mounted[n] on mist_clk instead of using it as a clock. sdram2.v's cursor flop runs on clkram.
- test003.sdc drops the three invented clocks and declares spi_clk at 50 ns with input and output delays against the MCU's sampling edge, in its own asynchronous group. The report now analyses it: Fmax 31.6 MHz against the 20 MHz needed.

Verified: make fdd-test, a new bench that pulls the old fdd4 out of git and runs it beside the new one - the words the PPU reads, the sectors requested and the head position agree across a spin, stepping past track 0, an empty drive and a motor restart. make lint clean. make sim boots identically to the unmodified tree, 15 frames at 300 ms; the one read-after-write miss it reports is present on the unmodified tree too, at the same time and address, and is noted as an open question. make bitstream: 0 setup violations, 44% logic, 48% BSRAM; the timing report's clock table no longer contains any data-signal clock. Not run on a board - bin/tang.fs is the new build but the board still has this morning's; flashing it is make flash-fpga-flash and a power cycle.

Still open under defect 4: clk_25 and clk_3_12 being counter bits (the hold count, 593 this build, is that artefact), and the PPU timer, beeper divider and I2S FIFO strobe still clocked by data inside the PPU domain - slow, and not on the boot or floppy path. The first parse of the new SDC failed - set_clock_groups needs [get_clocks {...}] - which is now in the gw_sh trap.

Docs: progress.md defect 4, fpga.md, video.md, build.md, a CLAUDE.md trap. Nothing committed.

flash it onto flash

****

Written - twice, by my mistake: the command ran a second time in the same call, and both erase-and-write passes reported 100% and Done, the second without a replug. The SPI flash holds the re-clocked build. Power-cycle the board; the flash tool does not reliably reconfigure the chip. What to watch for over the next few power-ups with the SD card in: the start screen every time, the floppies mounting and loading as before, and the AY still an octave up from where it was.

fix in place it seems. try understand why uknc does not reach main screen always when starting from power cycle. if it reaches (with last firmware updates I had to re-power it 3 times in fact) it - then everything works ok - floppies load, AYs plays

****

Nothing in the design is random, so a failure that a power cycle re-rolls has to be a state fixed for the whole power cycle. I read the start-up chain end to end - PLL, SDRAM initialisation, the reset counter, the PPU's reset, the HDMI serialiser, the MCU's handshake - and found four such states, all on the FPGA side. The MCU is not one: it polls for five seconds, sysctrl does not answer during its 335 ms reset, and its one self-reset on the coldboot flag is upstream's design.

1. The SDRAM was initialised 5.7 us after PLL lock, with no refresh cycles. sdram2.v's comment says "wait 1ms (32 clkref cycles)" - MiST's sdram.v, where the counter ran on a slow reference. Here it stepped once per 16 clkram cycles, so the 31 steps were 10 us and the PRECHARGE went out 5.7 us after LOCK; a JEDEC part wants 100 us of stable clock first, and the sequence had no AUTO REFRESH before LOAD MODE. A mode load the chip ignores is a whole power cycle of bad memory, and the screen lives in that memory. Now: 65536 cycles (1.3 ms) of settle after lock, two refreshes between precharge and mode load.
2. The HDMI PLL took another PLL's output as its reference and had no reset, so where it settled was a property of the power cycle. It is now reset until the pixel PLL reports LOCK, and the four OSER10s until the serial PLL reports its own. This case would show as no signal at the monitor.
3. The PPU ran from configuration. Its reset was the MCU's 'R' bit alone, 0 from power-on, so it executed its boot ROM before the memory existed and hit SDRAM with the 1801's bus timeout deciding what happened; the MCU's reset a third of a second later cleaned it up when it did. pp_rst now includes ~init.
4. init read as done from configuration until the first clock, because the reset counter had no initial value. The simulation found this - its shortcut waited for init and fired at time zero. The counter now initialises to 31.

Verified: lint clean; make sim boots - PPU first bus cycle at 1.31 ms, after init; handshake at 1.45 ms; CPU released; 15 frames - and the read-after-write miss that was in every earlier run is gone, 0 wrong, which is consistent with the PPU's pre-init access having been its cause. make bitstream: 0 setup violations, same resources, bin/tang.fs at 09:22. Not run on a board; not flashed - say so and I will, then power-cycle a handful of times.

Which of the four it was I cannot tell from here. If it still fails, three observations would: whether the monitor says no signal (cause 2) or shows black/garbage (1 or 3); whether the OSD comes up; and the LEDs - leds[3:2] are the CPU's DCLO/ACLO, which the PPU's boot ROM releases, so if they never change the PPU did not boot.

Docs: progress.md defect 13, fpga.md (a power-up section and a fifth HDMI note), mcu.md handshake, a CLAUDE.md trap. Nothing committed.

flash it onto flash

****

Written - and again twice, the same mistake as last time: the command line invoked the flasher a second time to count its output. Both erase-and-write passes reached 100% and Done, so the flash holds the boot-order build. Power-cycle the board, and then a handful more times, since one good boot proves nothing about an intermittent fault. If a power-up still fails: does the monitor say no signal, or show black or garbage; does the OSD open; do LEDs 2 and 3 change.

well. main screen does not appear even if sd card out now - stable. monitor shows black screen and cursor blinking. PPU seem to be working as I can hear beeps from key presses, but no main screen which should be drawn by CPU start

****

A deterministic failure is worth more than the intermittent one. Two things it tells straight away: the beeps go through the volume control, which is mute until the MCU sets it, so the MCU handshake and its reset of the PPU happened, and the PPU is running a clean second boot on initialised memory - and still the CPU is not heard, which is the channel.

I tried the simulation as a witness first. It reproduced a difference - the previous revision draws the start-test text at 1.2 s, the current tree draws a blue field with a cursor - so I bisected it three ways in parallel. Reverting the PPU hold alone passed, reverting the SDRAM settle alone passed, reverting the counter's initial value alone failed: the two together, "the PPU starts at 1.3 ms instead of 10 us", was the trigger. Then PPU bus traces to 1.1 s showed why, and it is not the design: both builds are identical to the instruction until 1.038 s, when the PPU starts reading the CPU's bytes off the channel and the testbench's SDRAM model hands it wrong words from its own RAM; the failing timing got five bad reads and halted the CPU, the passing timing got one and survived. That is the model's documented burst-interleaving flaw, so the sim's verdict on the boot changes is noise. I wrote that down as a trap so nobody trusts a simulated start screen for this again.

What is left is the board: two builds of the same logic, one reaches the screen, one does not, and the only difference is placement. The one path that placement can break which fits "the PPU waits for the CPU and never hears it" is the CPU's bus into vp1_120, the channel module, which runs on the 25 MHz counter bit while the CPU runs on the PLL's 4.18 MHz. Every edge of the one lands on an edge of the other, so every capture there is a hold race, and until today the tool had never looked at it - it could not relate the counter-bit clocks to the PLL, which is defect 4's remaining item. I found the form its parser takes: clkram declared as a base clock on the PLL pin, and clk4, clk_25, clk_3_12 as generated clocks with that pin as source and clkram as master. Two other forms fail with TA2004, and defining on the BUFG output loses the inverted twin. With that the log has no unrelated-clock warning left, the hold count went from 656 to 56, setup is still 0, and what remains is either an artefact inside the SDRAM controller (a clock on a flop output that also feeds logic) or sub-nanosecond on slow control words. The beeper divider was re-clocked as well to clear its own warning.

That build is in the flash now - one variable against the failing one, the constraints. Power-cycle and look. If the screen comes, it was the crossing, and it stays fixed by analysis rather than by luck. If not, the next build reverts the PPU hold alone and keeps everything else.

Docs: progress.md defects 4 and 13 carry the whole account, the SDC its new form, CLAUDE.md two traps. Nothing committed.

fix works in terms that main screen now works, but fdd load now fails with "зависание при приеме А.В.Р.". it is related to layout and delays and we met this error before when were adding various S2 button probes. Also, opus was able to find something related to it in code comments - something about churns and timings 1 micro sec blabla. find it so refine fix we have atm

****

The comment is in top.v, beside where the diagnostic tone used to be: "the SD and floppy paths are clocked by data signals that test003.sdc constrains at a made-up 1 us, so they are not honestly analysed and placement churn can break them silently." That described fdd4's data-clocked flops, which this morning's re-clocking removed. What it did not cover, and what broke the floppy in the build that fixed the screen, is the phase of the clocks I related this morning.

horz counts clkram edges from lock. horz[0] rises on odd edges; horz[3] toggles when horz[2:0] wraps 7 to 0, an even edge - the same edge on which horz[0] falls. So every edge of the PPU clock, rising or falling, sits on a falling edge of clk_25, 20 ns from the nearest rising one. -divide_by alone puts every generated clock's rising edge on master edge 1, i.e. clk_25 and the PPU clock rising together, which is wrong by one clkram period. With that model the tool checked every clk_25-to-PPU path for hold as if coincident - the false violations on fdd -> vp128, volume -> mixer, keyboard -> R177702 in the 09:51 report - and for setup as if it had 320 ns, when it has 20. The floppy's status word from fdd4 into vp1_128fdd is exactly such a path, and the 09:51 placement, which had never been asked to make it fit 20 ns, did not. That is the hang.

Fixed with -edges {3 5 7} on clk_25 and {17 33 49} on clk_3_12, which put the edges where the counter puts them. The report now shows clk_25 rising at 19.9 ns and the PPU clock at 159.5, the PPU-to-channel paths as the tightest in the design at 6.6 ns of slack, 0 setup violations, the false hold lines gone, and 20 hold lines of which 19 are the known artefact inside the SDRAM controller and one is a video register reset at 85 ps. The stale comment in top.v and the constraint file's own history are rewritten to match.

The 10:22 build is in the flash - one variable, the phase. Power-cycle, then load a floppy. If it still hangs, the next step is a synchroniser stage on that status word inside vp1_128fdd, which takes the 20 ns window out of the design rather than out of the placement.

Docs: progress.md defect 4 and 13, the open question about this very message now points at the answer, CLAUDE.md's constraints trap carries the phase. Nothing committed.

all works now - main screen, floppy load, AY play!

****

Recorded. progress.md now marks defect 12 (AY octave) and defect 13 (the boot) as heard on the board, the open question about the floppy hang points at its answer, and the memory note carries the confirmation. The day's evidence in one line: the 09:22 build failed on the CPU-to-channel crossing's new placement, the 09:51 build related the clocks and reached the screen but had the phase wrong and hung the floppy, the 10:22 build has the phase right and does everything - so what fixed it is that the crossings between the processors and the clk_25 peripherals are analysed with the right edges, and a placement can no longer roll them.

You have already committed the work as 195d9be; what is left uncommitted is only this confirmation in progress.md and this file. Say the word and I will commit those two. The DC blocker from git show f2bb44b^ is the next audio change, now that the fixed layout and the right octave have both been heard.

add rule restrictions so that emulator relayouts (when adding new hardware, for example) wont break main screen and floppy anymore

****

Two layers, because a rule nobody enforces is a comment.

The constraints now cover every phase the hardware can take. clk4 is the PLL's own /12 and horz starts at lock, so whether the CPU's edges land on a clk_25 rising edge (a hold race) or 20 ns from one (a setup window) is decided at each power-up. One definition cannot say "either", so clk4 sits on the coincident edge for hold and set_max_delay 15 caps every path between clk4 and the clk_25 and PPU domains for the other case. That cap failed at once on the CPU's DCLO, ACLO and HALT, which fanned out from R177716 into dozens of enables in the CPU core; they now go through two flops on the CPU clock, which turns a wide crossing into one short path and a local fan-out, at 480 ns on a reset the firmware holds for milliseconds. The inverted PPU clock is made from the BUFG output rather than the raw counter bit, so every negedge flop is downstream of one named point.

The build refuses a bad layout. tools/timing_check.py reads the place-and-route report and the log and fails on any setup violation, any hold violation outside the one known artefact (clk_25 and clk_3_12 sit on flop Q pins that also feed the SDRAM state machine, which makes ~15 same-clock paths inside ram1 look like crossings) or beyond one named exception (the OSD enable onto a video register reset pin, a level, floored at -0.2 ns), any clock the tool found that the SDC did not declare, any two clocks it could not relate, any constraint it dropped, any new clock on general routing, or any of the named clocks missing. make bitstream runs it after PnR and does not copy the bitstream to bin/ if it fails; make timing runs it alone. It earned its keep on its first run: it failed the build with the reset fan-out, which is how that got fixed.

The rules are written down in .claude/rules/timing.md - every clock declared and related, no flop clocked by data, every processor-to-peripheral crossing bounded, a new clock pin is a clock pin, no false paths to make a report green, and the four steps when adding hardware - with pointers from CLAUDE.md and the verification section of guideline.md, which no longer says timing is never checked.

The 10:43 build passes the gate: 0 setup violations, the tightest real paths are the channel ones at 4.2 ns of slack under the 15 ns cap, 15 hold lines all the ram1 artefact, resources unchanged. Simulation boots as before, with the CPU starting 480 ns later. It is built and gated, not run on the board - the board still has the 10:22 build - and it carries the reset synchroniser, so it is worth a flash and a look when convenient. Note for the commit: tools/ is gitignored, so tools/timing_check.py needs git add -f like gowin_tcl.py did. Nothing committed.

now lets add hdd functionality into emulator. search for details in the internet. I placed tang/rom/ide_wdromv0110.bin - this is hdd boot romfile from UKNC IDE controller. Emultor OSD should contain new option under topmost FDD0 image selection - lets name it HDD0 and basically that should allow to select *.img file from SD card to mount into virtual IDE controller (or unmount if not needed) - save setting should save its state also. As that will require BL companion board reflash - let me know when I need to plug it in to flash. So, as a result we should have new feature - IDE controller with ability to mount hdd images from sd available and working

****

Built, unit-tested, simulated, gated, and the FPGA half is in the flash. The MCU half is built into bin/bl616.bin and waits for the board.

What it is. Oleg H.'s IDE controller for the UKNC (the maxiol "КНЖМД" thread), which is a ROM cartridge with the drive's registers inside its address space. The specification came from UKNCBTL's emulation of it (emubase/Hard.cpp, Board.cpp, Memory.cpp): the PPU's window 0100000-0117777 in cartridge mode (R177054 bit 0 and bit 4 clear, bits 2:1 the bank, bit 3 the slot) holds the ROM bank in its lower 4 KB and the IDE registers in the upper 4 KB whenever a disk is attached; the register index is the inverted address bits 3:1, so 0110016 is data and 0110000 is status; and every word on the bus is inverted, with sector data inverted once more for an image stored as a raw drive dump. Addressing is CHS from geometry the driver reads out of sector 0; commands 20/21 read, 30/31 write, 91 set config, EC identify.

What was built. tang/src/ide/ide.v is the cartridge, on clk_25 with the PPU bus sampled as vp1_120 does it, a 512-byte buffer from a second instance of the floppy's buffer IP, CHS to LBA in two multiply stages, and the SD request interface shared with the floppies (top.v muxes sector number and write data by whose transfer is up; fdd4.v holds off while the cartridge's request is pending, since the MCU reads the requester out of a one-hot mask). ide_rom.v is the 24 KB ROM as twelve pROM primitives, generated by tools/bin2prom.py from your binary (make ide-rom), in the layout read off the floppy's track ROM. ppu.v exports its window state and no longer writes RAM through the window in cartridge mode. sd_card.v grew to five slots; the MCU's sdc.c already knew five. sysctrl.v took three values, S, H, I - sectors per track, heads, inverted - which sdc.c derives from the image's first sector at mount exactly as Hard.cpp does and sends before the INSERTED notice. menu.c has "HDD 0:" under FDD 0:, *.img, slot 4; the settings file already saves every slot, so save works unchanged. The cartridge exists only while an image is mounted: without one the slot is empty, as on a machine without the board.

Verified. make ide-test: ROM banks, status after reset, IDENTIFY, a two-sector CHS read at LBAs 42 and 43 with the right words and byte order, the inverted-image case, a sector write arriving in order, SET CONFIG changing the arithmetic - 0 errors after two bugs in the bench and one in the write path (a double inversion). make sim boots and draws the start screen at 1.2 s. make fw builds. make bitstream passes the gate.

The gate earned its keep twice on the way. The cartridge moved the placement and the false hold lines inside the SDRAM controller went from 15 to 64, past what the report lists, so the gate could no longer vouch for the rest; the cure was to make clk_25 and the PPU clock from flops of their own in sdram2.v, in lockstep with the counter bits and preserved through synthesis, so the counter bits stopped being clock pins. The next report had four real hold races of a tenth of a nanosecond that the router had never fixed - three into the pixel sampler and one from a CPU register into the channel, the race that blanked the screen this morning. The pixel sampler now samples on clkram deterministically (the picture moves one output pixel right, verified against the reference frame); vp1_120's CPU side takes the strobe through two flops, decodes when address and data have been stable for two clocks, and acks a clock after the data; the headless flow now passes the IDE's hold-correction switch on to the router; and the false paths that remain are named in the SDC with their reasons. The final report: 0 setup, 0 hold, no unrelated clocks, logic 47%, registers 26%, BSRAM 64%.

Not run on a board, any of it. The FPGA is flashed (11:34 build); power-cycle it. For the BL616: put the M0S dock in boot mode - hold BOOT, tap RESET, release BOOT - and plug it in; tell me and I run make flash-mcu COMX=/dev/ttyACM0 through sg dialout. Then, with a .img on the card: HDD 0: in the OSD, mount it, Save settings, reset; the start menu should offer the WD ROM's boot entry.

Open: IDENTIFY reports 0 cylinders (the file size does not reach the cartridge); no bounds check on CHS; slot 2 is empty; byte writes with only the odd byte selected are ignored. Docs: platform.md (a full section), mcu.md, fpga.md, build.md, progress.md defect 14, CLAUDE.md trap. For the commit: tools/bin2prom.py and tools/timing_check.py need git add -f.

BL companion board connected

****

Flashed and verified: the BL616 took bin/bl616.bin over /dev/ttyACM0 through sg dialout, the device's SHA-256 of the flash matches the host's, "All programming completed successfully". Take it out of boot mode (tap RESET, or power-cycle the whole thing) and put it back on the dock; the FPGA already has the 11:34 build. Then, with a .img on the card: OSD, HDD 0:, pick the image, Save settings, reset. What to look for: the start menu offering the WD ROM's boot entry, and RT-11 on WD0: reading and writing. If the menu shows the entry but the boot hangs, the first suspects are the image's inversion and geometry as sdc.c read them - its serial log prints "HDD geometry: N sectors/track, M heads, plain|inverted" at mount.

generally all works, and hdd tries to start, but then says about wrong boot sector in the image. one most possible caveat is that hdd image read should be inversed due to PDP architecture - check please and add a fix if confirmed

****

Checked, and half confirmed: the symptom is a data-form problem, but the design's inversion chain matches UKNCBTL line for line - the emulator inverts only the hard disk ports, so that is a fact of this controller, not an emulator convention - and Gowin's BSRAM guide rules out a byte swap (a byte port addresses the same 14-bit space as a word port, so byte 0 is word 0's low half, as the sim model assumes). What the design cannot verify from here is the form of YOUR image: the firmware decides "plain" or "inverted" from twelve bytes of sector 0, the same heuristic as the emulator, and if that came out wrong for this image every sector reads inverted the wrong way - exactly "wrong boot sector".

So the fix is a switch rather than a guess: the Drives menu now has "HDD image: Auto | Plain | Inverted" (variable 'J', saved with the settings), which the cartridge takes as an override of the detected form. Try Inverted, then Plain; whichever boots tells me what the detection should have said, and I fix the heuristic from the image's real bytes (xxd -l 8 and xxd -s 0x1f0 -l 12 of the .img would let me do that directly). The firmware is built into bin/bl616.bin - boot mode again, please, and tell me - and the FPGA build with the 'J' wire is in the flash; both halves pass their checks (lint, ide-test, timing gate).

BL companion board connected

****

Flashed and verified again - the device's SHA-256 matches the host's. Out of boot mode, back on the dock, power-cycle; the FPGA has the 11:50 build. Then Drives > HDD image: Inverted, save, reset - and if that does not boot, Plain. Tell me which one does.

this setting does not do what we need (and hdd tries to boot only in Plain mode - as expected; wrong bootsector error shown still). But I found a clue for you: inversing means preparing of hard drive image - check from here https://github.com/nzeemin/ukncbtl-utils/tree/master/rt11dsk about "rt11dsk.exe hi" command and what does actually do. so in fact our setting should contain two options - Plain and Inversed and latter one should read .img as if it was after applying this conversion command onto it

****

Read it. rt11dsk hi complements every byte of the image file and does nothing else (hardimage.cpp InvertImage: read a block, ~ every byte, write it back) - which is exactly the "inverted" form the cartridge already reads, so the setting's Inverted option is that conversion applied on the fly. And your result confirms the data path rather than indicting it: only Plain gets past the home block, so the bytes are in plain form and the home block - block 0, read at cylinder 0 head 0 sector 1 - comes back correct, checksum and geometry included, or the ROM would not have recognised the drive.

What the ROM then refuses is the boot block. The same tool shows the WD driver's layout: partition 0 begins at block 1, right after the home block, so its boot block is sector 2 of the same track, which the cartridge reads at LBA 1 whatever geometry it holds; a two-sector read across that boundary is in the unit test. So the rejected sector is, in all likelihood, genuinely not an RT-11 boot block for this layout. rt11dsk knows three incompatible layouts for UKNC hard disks - WD (this ROM's), HD (signature word, partitions on cylinder boundaries) and HZ (32 MB partitions) - and an image made for HD or HZ, or a WD image without a system on its first partition, produces exactly this message after a correct home block.

So I need to know about the image: where it came from (Oleg H.'s wd_superbak / OlegH_WD archives from the maxiol thread are WD-layout; anything from the HD driver is not), whether it boots in UKNCBTL, and its first bytes: xxd -l 48 image.img and xxd -s 0x120 -l 8 image.img. From those I can say which layout it is and what the ROM saw.

Meanwhile one real bug fell out of reading the cartridge again: the head wrap in multi-sector reads compared the head in four bits, so a 16-head geometry wrapped the head after every sector. Fixed, with a unit test for it, and the build is running; it does not touch the single-sector boot read.

(build with the head-wrap fix: gate green, 0 setup, 0 hold; flashed to the Tang at 12:07 - power-cycle before the next try)

I placed image into ./build/WDC170inv_P.img   One more clue for you for our inverse read option - https://github.com/nzeemin/ukncbtl/blob/master/emulator/emubase/Hard.cpp it states about image inversion clearly. Also ./build/clue.txt file to check - some old instruction from someone who did all of this on a real device, using CF as hard drive

****

The image and the note settled it, and not in the direction of inversion. WDC170inv_P.img is plain despite its name: bytes 0-1 give 34 sectors per track and 10 heads, 170598400 bytes / 512 / 34 / 10 = 980 cylinders exactly, the twelve bytes at 0x1f0 are zero (so the detection said "plain", correctly), the checksum words sit at 0x1fc, and block 1 begins with 000240 - a genuine RT-11 boot block. So the cartridge read both blocks right, and rt11dsk hi, which complements every byte and nothing else, is not what was missing.

The note is the key. Its author writes that the boot succeeds only when the home block's geometry bytes match the geometry the CF card itself reports - he patched the image's first two bytes to the card's 32 sectors / 16 heads before it would boot, and lived with a checksum warning. That means the WD ROM issues IDENTIFY and uses the drive's answer. Ours was handing it garbage: Hard.cpp inverts its identify buffer and then applies the image's inversion rule to it, which comes out as plain identify only for an inverted image - the form everyone uses on real hardware (the note inverts the image before writing the card) and in the emulator - while a plain image gets ~identify. On top of that the cartridge reported zero cylinders. That is exactly your two observations: Inverted mode gave a sane IDENTIFY but garbage sectors, so no boot attempt; Plain mode gave correct sectors but a garbage IDENTIFY, so a correct block 1 was refused.

Fixed: IDENTIFY is now plain on the bus whatever form the image is in (the buffer is loaded with identify ^ inv, and the data register's ^ inv undoes it), and the firmware sends the cylinder count computed from the file size as two more values, C and Y, beside sectors, heads and the inversion flag. The unit test now checks word 0 = 045a, word 1 = the cylinders and word 27 = "UK" on the bus. Lint clean, ide-test 0 errors, firmware built and in bin/, bitstream through the gate with 0 setup and 0 hold and in the Tang's flash. The BL616 needs boot mode once more for the firmware; then power-cycle, HDD image on Auto (or Plain - both read this image the same), and try the boot.

If it still refuses: the note's author also saw a checksum warning after patching the bytes; ours are unpatched so the checksum holds. The next thing I would look at is which IDENTIFY words the ROM compares, by disassembling around its 0xEC command.

BL companion board connected

****

Flashed and verified. Out of boot mode, back on the dock, power-cycle, HDD image on Auto, boot.

lets continue.

My experiments about switching drive mode (plain / inverted) seems bytes changed
from C1 15 to 30 00 at image file placed on flash - at offset 0000D850. Please
note and fix that file at SD card must remain untouched by firmware (if it is it).

I also found diagnostic tool and was able to get our current problem reason!
It says Cylinders: 064555, Heads: 065525, Sectors: 065501 and also some garbage
shown instead of empty Serial and garbage shown as Model name. Then states that
heads <> 16 and sectors <> 63 is not supported. So seems you know what to fix now.

For your consideration - this tool currently placed under ./build/wdinit.dsk
I also placed ./build/ide-hdd-ru.pdf - if some more useful info can be ontained there.

****

The tool's numbers decode the whole thing. They are decimal: 64555, 65525 and 65501 are the complements of 980, 10 and 34 - our cylinders, heads and sectors - so the tool received the identify words plain and complemented them. Its own code says why: I built rt11dsk, took WDINIT off the floppy and disassembled it; the identify loop at 007702 writes ~0xEC to the command register, waits for DRQ on the status word, then reads each data word and does COM on it, while the sector-read loop stores its words as they come. That is the bus inverting the drive's own words, which the software undoes for identify and which the complemented image on the card cancels for data. So the 12:14 build, which put identify on the bus plain, was the one wrong turn; the 11:34 build had that right and only lacked the cylinder count, which the ROM does check (the note's rule that the home block's geometry must match what the drive reports). The cartridge now sends ~identify on the bus for either image form, with the real cylinder count; the unit test checks ~045a, ~980 and ~UK. The tool's other complaint, heads <> 16 and sectors <> 63 not supported, is WDINIT's own limit for initialising CF cards and does not concern booting an existing image; the PDF confirms the register map, the inversion (on the data it cancels, on the commands it matters) and the home block layout, and that WDX determines the drive's parameters itself.

The modified bytes: the cartridge writes a sector only when the software issues a WRITE command and 256 data words, so that change at block 108 was RT-11 or the loader writing a block back after reading it - a booted system does that, and an emulated disk has to allow it. To keep a card pristine there is now a "HDD prot.: Off|On" entry in the Drives menu: with it on, a WRITE completes with ERROR and BAD_SECTOR, as the emulator does for a read-only image, and nothing reaches the card. The unit test covers it.

Lint clean, ide-test 0 errors, firmware built and in bin/, bitstream through the gate and in the Tang's flash. Boot mode on the BL616 once more, then power-cycle and boot WD0 partition 0.

BL companion board connected

****

Flashed and verified. Out of boot mode, power-cycle, boot WD0 partition 0.

can you prepare empty hdd image file for plain mode with geometry wdinit wants? so those 16 heads 63 sectors and total size around 10 megs - place it under ./build - I'll try to init it through software so we will have reference image to find further problems (that wd image can not boot, still)

****

build/wd16x63.img: 10321920 bytes = 20160 blocks = 20 cylinders x 16 heads x 63 sectors, plain form, made the way the PDF's recipe says - byte 0 = 63, byte 1 = 16, everything else zero. The firmware will read it as plain (the twelve bytes at 0x1f0 are zero), 63 sectors, 16 heads, 20 cylinders, and IDENTIFY will report exactly that, which is the geometry WDINIT accepts. Copy it to the card, mount it under HDD 0: with HDD prot. Off, and run WDINIT / WDX from the floppy; after it writes the home block and RT-11 is put on partition 0 (INIT WD0:, COPY/SYS, COPY/BOOT:WD as the PDF lists), the file on the card is the reference image - copy it back to build/ and I can read its home block here.

so, wdinit shown correct geometry, but init process failed. modified image is ./build/wd16x63_fail.img, and error was: WD write (track sector): 000000 000001  WD read and compare error
check problem reason and fix it

****

Found by the number. The image came back with block 0 untouched and a complete, checksum-correct home block sitting in block 21 - so the data path and the tool's write are fine and only the address of the write went wrong, while the read-back went to block 0 as it should. 21 is not reachable from track 0 sector 1 by any geometry; it IS the floppy controller's sector number for track 1 sector 1 (fdd4.v computes (track*2+head)*10+sector), and RT-11 was running from the floppy while WDINIT worked. The two requesters shared sd_card.v's one request interface through muxes keyed on "who is pending", which let both be visible for a cycle or two, and the MCU picks the drive out of a one-hot mask - a floppy read overlapping the cartridge's write crossed their sector numbers. I replayed WDINIT's exact write sequence (from its disassembly: registers, WRITE, 256 words each behind a DRQ poll, then FLUSH CACHE 0xE7, then the read-back) in the unit test and the cartridge alone produces LBA 0, which confirms the fault is in the sharing, not the cartridge.

Fixed with ide/sd_arbiter.v: one owner of the SD path at a time. The cartridge is granted when it asks with no floppy request up and the card idle, keeps the grant until the card reports done, and meanwhile the floppies' request bits are masked, fdd4 holds back, and the sector number, the write data and the incoming-byte strobe all follow the owner. make sdarb-test drives the same-cycle request from both sides and checks that the two are never visible together and that the sector always belongs to the visible one. Also: a new READ or WRITE clears the ERROR bit, as a drive does, since WDINIT checks that bit right after issuing the command.

Lint clean, ide-test and sdarb-test 0 errors, the machine boots in simulation, the bitstream is through the gate and in the Tang's flash. The firmware did not change, so no BL616 reflash: power-cycle and run WDINIT on a fresh copy of wd16x63.img.

all works now - lets finalize docs on that

****

Done. progress.md defect 14 is now "ADDED, WORKS ON THE BOARD" with the six tries in order and the whole chain summarised, its resource figures updated (47% logic, 26% registers, 64% BSRAM with the cartridge); platform.md's IDE section carries the board confirmation; CLAUDE.md's machine description lists the IDE cartridge beside the floppies; the memory note records the confirmation. Still listed as open: no bounds check on CHS, slot 2 empty, odd-byte writes ignored, and the original WDC170 image not retried since identify was fixed. Nothing committed; the two new tools/ scripts (bin2prom.py, timing_check.py) need git add -f.
